# Legacy React / Inertia frontend (deprecated)

This folder is **no longer used**. The application runs on **Blade + Alpine.js** under `resources/views/`.

You may delete the entire `resources/js` directory after confirming you no longer need reference implementations.
