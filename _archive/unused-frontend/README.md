# Archived frontend (not used by CHTM)

This folder holds the old **React + Inertia + Vite + Supabase** stack. The running app uses **Laravel Blade**, Alpine.js, and `public/js/room-calendar.js` only.

Nothing in the live application loads files from here. You can delete this entire `_archive` directory once you are sure you no longer need the reference code.

Archived on: 2026-06-04

| Subfolder / file | Original location |
|------------------|-------------------|
| `spa-react-inertia/` | `resources/js/` |
| `node_modules/` | project root |
| `package-lock.json` | project root |
| `config/` | `tsconfig.json`, `tailwind.config.js`, `postcss.config.js` |
| `resources-css/` | `resources/css/` |
